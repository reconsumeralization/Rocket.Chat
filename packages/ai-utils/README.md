# AI Utils

This package provides utilities to integrate simple AI features into Rocket.Chat.

Currently it exposes an `analyzeSentiment` function which performs sentiment
analysis of text using the open source `sentiment` library. This helper is also
used by the `analyzeMessageSentiment` DDP method.

